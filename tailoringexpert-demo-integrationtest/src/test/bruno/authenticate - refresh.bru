meta {
  name: authenticate - refresh
  type: http
  seq: 5
}

post {
  url: {{HOST}}{{auth.refresh}}
  body: json
  auth: inherit
}

body:json {
  {
    "userId": "{{USER}}",
    "refreshToken": "{{auth.refreshToken}}"
  }
}
